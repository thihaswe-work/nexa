{
  "name": "@nexa/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types, interfaces, and utilities for Nexa",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
